[project]
name = "edunixo-realtime-voice-agent"
version = "0.1.0"
description = "EDUNIXO Pipecat + Gemini Live realtime conversation engine"
requires-python = ">=3.11"
dependencies = [
  "pipecat-ai[google,daily,runner]==1.8.1",
  "python-dotenv>=1.0.1",
  "loguru>=0.7.3"
]

[tool.uv]
package = false
